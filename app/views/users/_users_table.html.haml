%table.table
  %thead.thead-dark
    %tr
      %th{scope: 'col'} #
      %th{scope: 'col'} Name
      %th{scope: 'col'} Surname
  %tbody
    - users.each.with_index(1) do |user, i|
      %tr
        %th{scope: 'row'}= rows_count + i
        %td= user.name
        %td= user.surname
