%table.table
  %thead.thead-dark
    %tr
      %th{scope: 'col'} #
      %th{scope: 'col'} Username
      %th{scope: 'col'} Line's name
      %th{scope: 'col'} Grade
      %th{scope: 'col'} Style
      %th{scope: 'col'} Date
      %th{scope: 'col'} Belayer
  %tbody
    - ascents.each do |ascent|
      %tr
        %th{scope: 'row'}= ascent.id
        %td= ascent.user.name + ' ' + ascent.user.surname
        %td= ascent.line.name
        %td= ascent.line.grade
        %td= ascent.style.upcase
        %td= ascent.date
        %td= ascent.belayer
